import { Rule } from '../types';

// Import individual security rules
// For now, export empty object since rules are being migrated
export const securityRules: Record<string, Rule> = {
  // Security rules will be populated here as they are migrated
};
