import { Rule } from '../types';

// Import individual accessibility rules
// For now, export empty object since rules are being migrated
export const accessibilityRules: Record<string, Rule> = {
  // Accessibility rules will be populated here as they are migrated
};